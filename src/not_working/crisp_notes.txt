Labels:

1	the main loop, loops over each character

10	Used if character isn't a letter
11	Used if the non-letter char is in a string
12	Used if the non-letter char is for a branch operation

20	Used if the character is a string toggle symbol (i.e. ")
21	Used if string mode is currently active
22	Used if string mode is currently inactive

Functions:

print (112 114 69 110 116)

Booleans:
T = Boolean True
F = Boolean False

value1=value2 - returns T if value1 equals value2, F otherwise
value1!value2 - returns T if value1 doesn't equal value2, F otherwise
value1>value2 - returns T if value1 is greater than value2, F otherwise
value1<value2 - returns T if value1 is less than value2, F otherwise



